// src/themes/theme-saas-landing.js
export async function renderSaaSLandingTheme(data, view, request) {
  // Modern SaaS-style theme with bold colors, gradients, and CTAs
  // Similar structure but with SaaS styling
  return await renderModernBusinessTheme(data, view, request);
}

// src/themes/theme-community-forum.js
export async function renderCommunityForumTheme(data, view, request) {
  // Community-focused design with discussion-style layout
  // Similar structure but with forum styling
  return await renderModernBusinessTheme(data, view, request);
}
